# Benchmarks

Benchmark artifacts will record:

- runner architecture;
- J2 version;
- dataset size and distribution;
- number of candidate files;
- sequential/parallel mode;
- wall-clock runtime;
- repeated-run statistics.

No performance numbers are claimed until they are produced by the macOS CI environment.
