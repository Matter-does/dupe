# Standalone stage probe: Discovery + Filter + Candidate Read and Hash
import "../../src/scan.j2"
import "../../src/hash.j2"

args = proc.argv()
root := "."
if len(args) > 1 {
    root = args[1]
}

files = discover(root)
candidates = size_candidates(files)
hashed = hash_candidates(candidates)
print(fmt("HASHED:{}", len(hashed)))
